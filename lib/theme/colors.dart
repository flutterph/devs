// Keep it simple
//
// There are alternatives to handling colors, eg. [flutter_gen](https://pub.dev/packages/flutter_gen)
// But since we are expecting beginners to contribution to this repo,
// we don't want them to start dealing with issues encountered in code generation,

import 'package:flutter/material.dart';

class ThemeColors {
  static const Color yellow = Color(0xFFF1C40F);
  static const Color yellowDark = Color(0xFFC8A519);
}
